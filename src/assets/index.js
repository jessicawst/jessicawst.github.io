import shuLogo from './shuLogo.png';
import aboutCurve from './aboutCurve.svg';
import zigZag from './zigZag.svg';
import heartWithDashes from './heartWithDashes.svg';
import logo from './logo.svg';
import selfPortrait from './selfPortrait.png';
import resumePhoto from './resumePhoto.png';
import workSlant from './workSlant.svg';
import star from './star.svg';
import latestWorkStar from './latestWorkStar.png';
import comingSoonStar from './comingSoonStar.png';
import contactPhotos from './contactPhotos.png';
import randomShapes from './randomShapes.svg';
import fbStar from './fbStar.png';
import instaStar from './instaStar.png';
import linkedinStar from './linkedinStar.png';

const ShuLogo = props => (
	<img className={props.className} style={props.style} src={shuLogo} alt="" />
);

const AboutCurve = props => (
	<img
		className={props.className}
		style={props.style}
		src={aboutCurve}
		alt=""
	/>
);

const ZigZag = props => (
	<img className={props.className} style={props.style} src={zigZag} alt="" />
);

const HeartWithDashes = props => (
	<img
		className={props.className}
		style={props.style}
		src={heartWithDashes}
		alt=""
	/>
);

const Logo = props => (
	<img className={props.className} style={props.style} src={logo} alt="" />
);

const SelfPortrait = props => (
	<img
		className={props.className}
		style={props.style}
		src={selfPortrait}
		alt=""
	/>
);

const ResumePhoto = props => (
	<img
		className={props.className}
		style={props.style}
		src={resumePhoto}
		alt=""
	/>
);

const WorkSlant = props => (
	<img className={props.className} style={props.style} src={workSlant} alt="" />
);

const Star = props => (
	<img className={props.className} style={props.style} src={star} alt="" />
);

const LatestWorkStar = props => (
	<img
		className={props.className}
		style={props.style}
		src={latestWorkStar}
		alt=""
	/>
);

const ComingSoonStar = props => (
	<img
		className={props.className}
		style={props.style}
		src={comingSoonStar}
		alt=""
	/>
);

const ContactPhotos = props => (
	<img
		className={props.className}
		style={props.style}
		src={contactPhotos}
		alt=""
	/>
);

const RandomShapes = props => (
	<img
		className={props.className}
		style={props.style}
		src={randomShapes}
		alt=""
	/>
);

const FBStar = props => (
	<img className={props.className} style={props.style} src={fbStar} alt="" />
);

const InstaStar = props => (
	<img className={props.className} style={props.style} src={instaStar} alt="" />
);

const LinkedinStar = props => (
	<img
		className={props.className}
		style={props.style}
		src={linkedinStar}
		alt=""
	/>
);

export {
	ShuLogo,
	AboutCurve,
	ZigZag,
	HeartWithDashes,
	Logo,
	SelfPortrait,
	ResumePhoto,
	WorkSlant,
	Star,
	LatestWorkStar,
	ComingSoonStar,
	ContactPhotos,
	RandomShapes,
	FBStar,
	InstaStar,
	LinkedinStar,
};
